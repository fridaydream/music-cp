module.exports = {
  useTabs: false,
  // tab 缩进大小,默认为 2
  tabWidth: 2,
  // 单行代码最大长度，默认 80
  printWidth: 180,
  // 使用分号, 默认 true
  semi: false,
  // 在语句末尾添加分号，默认 none，可选值 none|es5|all
  trailingComma: 'all',
  // 使用单引号, 默认 false(在 jsx 中配置无效, 默认都是双引号)
  singleQuote: true,
}
